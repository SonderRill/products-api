```bash
$ docker-compose up
```
http://localhost:3000/api/ swagger

migrations work in production mode

```bash
$ npm run migration:generate {name}
$ migration:run
```

http://localhost:3000/api/ swagger